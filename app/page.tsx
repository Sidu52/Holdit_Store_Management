import Link from "next/link";
import { Store, Users, ArrowRight } from "lucide-react";

export const revalidate = 3600; // ISR for the dashboard if needed

export default function Home() {
  return (
    <div></div>
  );
}